version: '3'

services:
  ladies_first_db:
    container_name: ladies_first
    image: mongo
    restart: always
    ports:
      - '27017:27017'
    # environment:
    # MONGO_INITDB_ROOT_USERNAME: root
    # MONGO_INITDB_ROOT_PASSWORD: ladies_first_una!
    # MONGO_INITDB_DATABASE: diana
